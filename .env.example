# 复制为 .env.local 并填入你的 Supabase 项目信息。
# 若留空，应用会自动以「本地存储模式」运行（数据存在浏览器，方便先跑通主线）。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 图片上传到的 Supabase Storage bucket 名称 (需在 Supabase 控制台创建并设为 public)
VITE_SUPABASE_BUCKET=note-images
